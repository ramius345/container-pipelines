apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: {{ .Values.projectname }}-dev
  name: {{ .Values.projectname }}
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: {{ .Values.projectname }}
    weight: 100
  wildcardPolicy: None
